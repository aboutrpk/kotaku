/*FTP FILEZILLA*/
host : sftp://35.240.133.166
user : rsa-key-20190416
password : kosong
port : kosong

/*memberikan hak akses pada folder*/
1. sudo chmod 777 -R /var/www/html/appmbrio/assets/images
For most Apache configurations, run the following restart command:
2. sudo service apache2 graceful


/* Unistall Aplikasi */
1. sudo apt autoremove namaaplikasi


/*rename file*/
1. sudo mv namafile filebaru

/*pindahkan file isinya*/
We want to move the contents of this folder to /usr/share/phpmyadmin

sudo mv phpMyAdmin-4.8.5-all-languages/* /usr/share/phpmyadmin

/*delete file*/
sudo rm /usr/share/phpmyadmin/phpMyAdmin-4.8.5-all-languages.tar.gz
sudo rm -rf /usr/share/phpmyadmin/phpMyAdmin-4.8.5-all-languages
sudo rm -rf /usr/share/phpmyadmin.bak

/*unistall apache*/
sudo service apache2 stop
sudo apt-get purge apache2 apache2-utils apache2.2-bin apache2-common
sudo apt-get autoremove



/* install mysql */
1. sudo apt update && sudo apt install mysql-server
2. sudo service mysql status
3. sudo mysql_secure_installation


sudo service mysql start
sudo service mysql stop 

/*cara donlot file*/
sudo wget https://files.phpmyadmin.net/phpMyAdmin/4.8.5/phpMyAdmin-4.8.5-all-languages.tar.gz

sudo wget https://github.com/backrpk/percobaan/archive/master.zip

sudo wget https://github.com/backrpk/percobaan/raw/master/assets.zip

sudo wget https://github.com/backrpk/percobaan/raw/master/appmbrio4.zip

sudo wget https://github.com/bcit-ci/CodeIgniter/archive/3.1.10.zip
/*ekstrak*/
sudo tar xzf phpMyAdmin-4.8.5-all-languages.tar.gz



delete direktori
direktori  rmdir namafile

/* cara buat backup */
sudo cp /usr/share/phpmyadmin/libraries/sql.lib.php /usr/share/phpmyadmin/libraries/sql.lib.php.bak

/* cara edit file biar lolos permission */
sudo nano /usr/share/phpmyadmin/libraries/sql.lib.php

sudo nano /etc/nginx/nginx.conf
# set client body size to 8M #
client_max_body_size 8M;

For RHEL/CentOS/Debian/Ubuntu Linux, try:
# service nginx reload





mysql> update user set authentication_string=PASSWORD('RumahMbrio@.,') where user='root';
update user set plugin="mysql_native_password" where User='root';



Sometimes it happens that the log is not filling any data, because the file doesn't exist. Create a log file:

touch /var/log/vsftpd.log

Then

service vsftpd restart

sudo /etc/init.d/apache2 restart


sudo apt update && sudo apt install install filezilla






isi folder /var/www/html$ ls
index.nginx-debian.html  info.php  phpmyadmin

/*install aplikasi unzip*/
1. sudo apt update && sudo apt install unzip
sudo apt update && sudo apt install p7zip-full
2. unzip CodeIgniter-3.1.7.zip
cara unzip untuk 7zip--> 7z x COCR2_100.zip

/*rename folder*/
Perhatikan sebuah direktori baru yang disebut CodeIgniter-3.1.6. Ubah nama folder ini menjadi codeigniter.
    mv CodeIgniter-3.1.6 codeigniter

/8Copy folder*/
Copy folder CodeIgniter-3.1.7 ke folder web root /var/www/html

/*.htcaccess*/	
	Sekarang buat file .htaccess di / var / www / html / codeigniter / dan letakkan konten berikut di dalamnya.

    RewriteEngine on
    RewriteCond% {REQUEST_FILENAME}! -f
    RewriteCond% {REQUEST_FILENAME}! -d
    RewriteRule. * Index.php?/$0 [PT, L]

Pastikan file .htaccess Anda diaktifkan dengan menyetel AllowOverride to All dan menambahkan Order memungkinkan, menolak dan mengizinkan semua file hosts virtual, atau konfigurasi apache global Anda.

    <Directory /var/www/>
            Options Indexes FollowSymLinks
            AllowOverride All
            Require all granted
            Order allow,deny
            allow from all
    </Directory>

	
/*Menghapus Program Melalui Terminal*/
1. dpkg --list
2. sudo apt-get --purge remove program pada jendela Terminal. Pastikan Anda menggunakan nama resmi berkas program sebagai pengganti kata "program", kemudian tekan tombol
3. atau sudo aptitude remove program.

